# dbt is a separate toolchain from the FastAPI backend (backend/requirements.txt) —
# only needed for working on dbt/models/**, so it stays out of the main
# requirements.txt that `make setup` installs for day-to-day backend dev.
#
# Verified working against this project's models on 2026-09-04:
#   pip install -r dbt/requirements.txt
#   make dbt-test
dbt-core==1.10.9
dbt-postgres==1.9.0
